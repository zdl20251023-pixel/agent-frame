# 复式扑克 PvE 排名赛 7.15 设计补充说明（压缩版）

## 1. 阅读说明

本文档是《复式扑克 PvE 排名赛 7.15 研发设计文档》的补充说明，用于配合主设计文档回答评审过程中最可能被追问的问题。

主设计文档负责说明：

```text
7.15 怎么做
主流程怎么跑
核心表怎么设计
排行榜怎么实现
断线重启怎么恢复
最终结算怎么保证正确
```

本文档负责说明：

```text
为什么这么设计
哪些边界不做
异常情况怎么兜底
哪些点还需要确认
```

本文档不是完整问题百科，重点保留高价值评审问题。
阅读时可以按关注点跳转：

```text
关心整体架构，看第 2 章
关心表设计，看第 3 章
关心参赛和计分，看第 4 章
关心断线恢复，看第 5 章
关心排行榜，看第 6 章
关心 Redis，看第 7 章
关心最终结算，看第 8 章
关心异常和不做范围，看第 9 章
关心待确认项，看第 10 章
```

---

## 2. 核心设计决策

### 2.1 为什么 MySQL 是唯一事实源？

结论：因为用户参赛、手牌分配、计分、恢复、排行榜、最终结算都需要持久化、事务保证和可复查。

说明：
7.15 的核心风险不是查询慢，而是比赛结果不一致，例如重复计分、重复入榜、服务重启后进度丢失、最终榜和计分结果不一致。MySQL 可以通过事务、行锁、唯一约束和持久化记录保证这些关键数据正确。

研发口径：

```text
所有比赛事实数据以 MySQL 为准。
Redis、前端缓存、服务端内存都不能作为最终判断依据。
```

---

### 2.2 为什么 Redis 不能作为排行榜事实源？

结论：Redis 可以提升查询性能，但不能承载最终可信比赛结果。

说明：
Redis 数据可能过期、被淘汰、被清空、重启丢失。如果 Redis 作为事实源，一旦缓存异常，就可能导致排行榜结果丢失或不可复查。

研发口径：

```text
Redis 只做当前榜分页缓存、最终榜分页缓存、可选我的排名缓存和活动结算锁。
duplicate_match_leaderboard_result 才是排行榜事实源。
```

---

### 2.3 为什么只恢复活动进度，不恢复单手中途状态？

结论：因为单手中途恢复需要动作日志和牌局快照，明显超出 7.15 交付范围。

说明：
如果要恢复到 flop、turn、river 或某个行动点，需要保存公共牌、底池、筹码、当前行动方、用户动作、AI 动作等状态。当前 7.15 的目标是 PvE 排名赛闭环，不是完整牌局恢复或回放系统。

研发口径：

```text
7.15 恢复到当前未完成手牌。
如果该手未完成，下次从这手开头重新打。
```

---

### 2.4 为什么当前榜和最终榜分开设计？

结论：因为当前榜是活动进行中的动态排名，最终榜是活动截止后的冻结排名。

说明：
当前榜会随着新用户完赛不断变化，不能提前固定 `rank_no`。最终榜在活动截止后生成，应写入固定排名，方便用户复查和后续问题排查。

研发口径：

```text
当前榜动态查询，不落库 rank_no。
最终榜结算后写入 rank_no、is_final、finalized_at。
```

---

### 2.5 为什么最后一手计分和入榜必须同事务？

结论：为了避免计分、完成状态、榜单结果三者不一致。

说明：
最后一手完成时，需要同时完成 `hand_score` 写入、`user_hand` 完成、`entry` 完成、`leaderboard_result` 写入。如果这些操作拆开，可能出现手牌已计分但没入榜，或者已入榜但计分不完整。

研发口径：

```text
最后一手 submit hand 内，计分、entry completed、leaderboard_result 必须同事务提交。
```

---

## 3. 表结构与数据模型

### 3.1 为什么 7.15 使用 8 张核心表？

结论：8 张表已经覆盖活动配置、牌局包、用户参赛、手牌分配、计分、排行榜和结算状态。

说明：
7.15 不做动作日志、牌局快照、排名历史快照、复杂计分规则版本，所以不需要额外表。

研发口径：

```text
8 张表支撑 7.15 主闭环。
复杂回放、快照、规则版本后续再扩展。
```

---

### 3.2 为什么 activity 绑定 hand_pack_id？

结论：活动绑定的是牌局包，牌局包再关联多手牌，手牌再关联 `drill_id`。

说明：
这样可以支持一个活动配置一组候选手牌，一个手牌被多个牌局包复用，一个牌局包被多个活动复用，结构更灵活。

研发口径：

```text
activity -> hand_pack -> hand_pack_item -> hand -> drill_id
```

---

### 3.3 为什么 play_hand_count 要做 entry 快照？

结论：`activity` 是配置源，`user_entry` 是用户参赛时的执行快照。

说明：
如果后台修改活动配置，已参赛用户的挑战手数不应该被影响。保存快照可以保证用户参赛后流程稳定。

研发口径：

```text
activity.play_hand_count 是配置。
entry.play_hand_count 是用户参赛时快照。
```

---

### 3.4 为什么 user_hand 是恢复和计分关键表？

结论：因为 `user_hand` 保存了用户本次活动实际分配到的 N 手牌。

说明：
`user_hand` 记录用户打哪手、按什么顺序打、当前手状态是什么。断线恢复、start hand、submit hand、重复计分兜底都依赖它。

研发口径：

```text
用户实际打哪些手，以 duplicate_match_user_hand 为准。
```

---

### 3.5 为什么 leaderboard_result 同时承载当前榜和最终榜？

结论：因为当前榜和最终榜本质是同一批完赛用户的结果，只是阶段不同。

说明：
活动进行中 `is_final = 0`，用于当前榜。活动结算后同一批记录更新为 `is_final = 1`，写入 `rank_no` 和 `finalized_at`，用于最终榜。

研发口径：

```text
同一张 leaderboard_result 表表达当前榜和最终榜两个阶段。
```

---

## 4. 用户参赛、手牌分配与计分

### 4.1 重复 join 为什么返回已有结果？

结论：重复 join 是正常幂等场景，不应该让用户感知为错误。

说明：
用户可能重复点击、网络重试、多设备同时进入。返回已有 `entry` 和 `user_hand` 能保持体验稳定，同时避免重新分配手牌。

研发口径：

```text
重复 join 不重新分配，返回已有参赛结果。
```

---

### 4.2 为什么用户参赛后手牌不能重新分配？

结论：为了保证排名赛公平性和恢复稳定性。

说明：
如果每次进入都重新分配手牌，用户挑战内容会变化，计分结果不可比较，也无法稳定恢复。

研发口径：

```text
join 成功后 user_hand 固定。
后续只读取，不重新生成。
```

---

### 4.3 必选手和随机候选手如何保证数量合法？

结论：join 前或活动发布前必须校验配置合法。

说明：
必选手数量不能超过 `play_hand_count`，可用手牌总数必须大于等于 `play_hand_count`，否则无法生成 N 手牌。

研发口径：

```text
fixed_count <= play_hand_count
available_hand_count >= play_hand_count
```

---

### 4.4 submit hand 是用户提交分数，还是服务端生成分数？

结论：不是用户提交分数，是客户端通知当前手已完成，服务端生成分数。

说明：
前端不能作为最终分数来源。前端最多传递必要的对局完成上下文，服务端基于 PvE 对局结算结果生成 `win_loss_bb` 和 `score`。

研发口径：

```text
submit hand 触发结算，分数由服务端生成。
```

---

### 4.5 如果客户端没有调用 submit hand，下次进来怎么办？

结论：服务端不会确认该手完成，下次进入仍从当前手开头重新打。

说明：
服务端只有收到 `submit hand` 并成功写入 `hand_score` 后，才认为当前手完成。如果客户端没调用成功，MySQL 中没有 `hand_score`，`user_hand.status` 仍然是 `playing`，因此 resume 会返回当前手。

研发口径：

```text
user_hand.status = playing 且无 hand_score，
表示该手未被服务端确认完成；
下次从当前手开头重新打。
```

---

### 4.6 为什么 hand_score.user_hand_id 必须唯一？

结论：为了保证同一用户手牌只能计分一次。

说明：
重复点击、网络重试、多设备并发都可能导致重复 submit。唯一约束是最终兜底。

研发口径：

```sql
UNIQUE KEY uk_user_hand(user_hand_id)
```

---

### 4.7 为什么重复 submit 返回已有计分结果？

结论：因为重复 submit 通常是正常重试，不是业务错误。

说明：
第一次提交可能已成功但响应丢失。返回已有结果可以让前端继续流程，避免用户感知为失败。

研发口径：

```text
查到已有 hand_score，直接返回已有计分结果。
```

---

### 4.8 为什么完成 N 手不能只看 completed_hand_count？

结论：因为进度字段不能替代计分记录完整性校验。

说明：
完成 N 手还必须有 N 条 `hand_score`，否则 `total_score` 不可信，排行榜也不可信。

研发口径：

```text
completed_hand_count 和 hand_score_count 双重校验。
```

---

### 4.9 并发 submit 唯一键冲突怎么处理？

结论：回滚当前事务后，查询已有 `hand_score` 并返回。

说明：
唯一键冲突说明另一个请求已经成功写入。此时不应直接报错给用户，应返回已有结果。

研发口径：

```text
唯一键冲突 -> rollback -> select existing hand_score -> return existing result。
```

---

### 4.10 后台修改 play_hand_count，已参赛用户是否受影响？

结论：不应影响已参赛用户。

说明：
用户参加活动时，`play_hand_count` 应保存到 `user_entry` 作为快照。已参赛用户继续按快照完成挑战。

研发口径：

```text
已参赛用户以 entry.play_hand_count 为准。
未参赛用户以最新 activity.play_hand_count 为准。
```

---

## 5. 断线 / 重启 / 继续

### 5.1 resume 为什么只根据 MySQL 状态判断下一步？

结论：因为 MySQL 是唯一可靠的恢复依据。

说明：
前端状态可能丢失或不可信，服务端内存会因为重启丢失，Redis 也不是活动进度事实源。只有 MySQL 保存了用户是否参赛、分配了哪些手牌、完成了哪些手牌、是否入榜。

研发口径：

```text
resume 查询 activity、entry、user_hand、hand_score、leaderboard_result 后返回 nextAction。
```

---

### 5.2 服务端重启后为什么还能恢复用户进度？

结论：因为关键进度都落在 MySQL 中，不依赖服务端内存。

说明：
用户参赛记录在 `user_entry`，分配手牌在 `user_hand`，计分结果在 `hand_score`，榜单结果在 `leaderboard_result`。服务重启后重新查询即可恢复。

研发口径：

```text
服务端重启不影响活动进度恢复。
```

---

### 5.3 playing 状态到底表示什么？

结论：`playing` 只表示用户进入过该手，不表示能恢复到单手中途状态。

说明：
`playing` 用于区分用户是否已经进入当前手，便于恢复、统计、排查问题。但 7.15 不保存单手牌中途状态，所以不能恢复到某个具体行动点。

研发口径：

```text
user_hand.status = playing 且无 hand_score 时，下次从当前手开头重新打。
```

---

### 5.4 为什么 resume 原则上只读？

结论：避免用户刷新或反复进入页面时意外改变服务端状态。

说明：
resume 调用频率可能很高，如果它承担写操作，容易造成状态误推进。写操作应由 join、start hand、submit hand 分别承担。

研发口径：

```text
resume 只返回 nextAction。
状态推进由 join、start hand、submit hand 完成。
```

---

### 5.5 未完成但活动已截止时，resume 是否更新 expired？

结论：不建议由 resume 更新；resume 返回 `activity_expired`，批量更新由 Worker 最终结算处理。

说明：
resume 是只读接口，保持职责清晰。未完成用户过期状态由最终结算统一处理，可以避免零散写入导致状态口径不一致。

研发口径：

```text
resume 遇到截止未完成返回 activity_expired。
Worker 结算阶段将未完成用户更新为 expired。
```

---

### 5.6 最后一手提交成功但客户端没收到响应，怎么恢复？

结论：再次 resume 或 submit 时返回已有完赛结果和榜单入口。

说明：
如果事务已经提交，`entry.status = completed`，`hand_score` 和 `leaderboard_result` 都存在。客户端没收到响应不影响服务端事实数据。

研发口径：

```text
resume 查询 entry completed + leaderboard_result 存在后，返回 show_current_rank 或 show_final_rank。
```

---

### 5.7 Redis 清空是否影响用户继续活动？

结论：不影响。

说明：
用户继续活动依赖 MySQL，不依赖 Redis。Redis 清空只影响排行榜缓存和结算锁状态。

研发口径：

```text
Redis 清空后，排行榜缓存可从 MySQL 重建。
```

---

## 6. 排行榜设计

### 6.1 为什么当前榜不提前落库 rank_no？

结论：因为当前榜排名会持续变化，提前落库会带来频繁更新和一致性成本。

说明：
如果每个用户完成 N 手后都更新所有人的当前排名，会导致大量写操作。当前榜只是活动进行中的临时排名，按排序条件实时查询即可。

研发口径：

```text
当前榜查询时按 total_score DESC、complete_time ASC 动态排序。
rank_no 只在最终榜阶段写入。
```

---

### 6.2 为什么最终榜必须冻结 rank_no？

结论：因为活动结束后，排名需要稳定、可复查、可展示。

说明：
最终榜是活动结果，应在截止结算后固定下来。用户后续查看第几名时，不应该因为查询时机、缓存状态或后续异常数据变化导致排名变化。

研发口径：

```text
Worker 结算最终榜时写入 rank_no、is_final = 1、finalized_at。
```

---

### 6.3 为什么用户必须完成 N 手后才进入排行榜？

结论：为了保证排名口径公平一致。

说明：
如果未完成用户也进入榜单，就会出现完成手数不同的用户互相比较，排名没有公平性。排行榜只能比较完成同一挑战手数的用户。

研发口径：

```text
未完成 N 手用户不进入当前榜，也不进入最终榜。
```

---

### 6.4 排序规则为什么是 total_score DESC、complete_time ASC？

结论：总分优先，同分时完成时间早者优先。

说明：
这是 7.15 第一版最简单、可解释、可实现的排序口径。更复杂的 tie-breaker 可以后续扩展，但不建议在 7.15 第一版引入。

研发口径：

```text
ORDER BY total_score DESC, complete_time ASC
```

---

### 6.5 total_score 是否确定为 AVG？

结论：当前建议是 AVG，但最终需要产品或计分规则确认。

说明：
单手 `score` 是 0-100，如果取平均值，`total_score` 也保持在 0-100 区间，便于展示和理解。但如果产品明确要总和、加权、去最高最低分等规则，应以产品规则为准。

研发口径：

```text
默认建议 total_score = AVG(hand_score.score)。
最终以产品确认的计分规则为准。
```

---

### 6.6 为什么当前榜和最终榜不是两张表？

结论：7.15 第一版没有保留历史当前榜快照的需求，一张表更简单。

说明：
两张表会带来数据复制和同步问题。当前设计中，最终榜就是对已完赛结果的冻结，不需要复制一份新数据。

研发口径：

```text
7.15 不复制最终榜数据。
如果后续要保留历史快照，再新增表。
```

---

### 6.7 我的榜单结果和我的当前排名有什么区别？

结论：我的榜单结果是是否入榜和成绩；我的当前排名是当前榜阶段实时第几名。

说明：
我的榜单结果可以直接通过 `activity_id + user_id` 查。当前榜不落库 `rank_no`，所以我的当前排名需要实时计算。

研发口径：

```text
我的结果必做。
我的当前排名按需计算。
```

---

### 6.8 为什么我的当前排名可能有性能问题？

结论：因为需要计算有多少人排在我前面。

说明：
当前榜阶段 `rank_no` 不落库，实时排名通常需要 COUNT 排名前面的用户。单活动用户量大时会有压力。

研发口径：

```text
默认不在列表接口中自动计算我的当前排名。
```

---

### 6.9 为什么不直接用 Redis Sorted Set 做主排行榜？

结论：7.15 优先保证正确性，Sorted Set 作为后续性能优化。

说明：
当前排序需要 `total_score DESC + complete_time ASC`，需要复合排序编码。为了避免一致性复杂度，第一版以 MySQL 为事实源。

研发口径：

```text
MySQL 是排行榜事实源。
Sorted Set 后续按规模评估。
```

---

## 7. Redis 缓存与结算锁

### 7.1 当前榜缓存什么时候生成？

结论：用户查询当前榜时生成。

说明：
缓存不是入榜时提前生成，而是查询时 Redis miss 后查 MySQL，再写入 Redis。

研发口径：

```text
query current leaderboard -> Redis miss -> MySQL -> write Redis。
```

---

### 7.2 为什么最后一手完成后要失效当前榜缓存？

结论：因为新用户入榜后当前榜可能变化。

说明：
如果不失效缓存，用户可能继续看到旧榜单。

研发口径：

```text
MySQL 事务提交成功后，递增 current leaderboard version。
```

---

### 7.3 当前榜缓存为什么用 version 方式失效？

结论：避免使用 `KEYS` 批量删除大量分页缓存。

说明：
分页缓存可能有很多 key。version 增加后，新查询会使用新 key，旧 key 等 TTL 自动过期。

研发口径：

```text
current:{activityId}:v:{version}:page:{page}:size:{pageSize}
```

---

### 7.4 最终榜缓存什么时候生成？

结论：用户第一次查询最终榜时生成。

说明：
最终结算只负责冻结排名，不负责预热所有分页缓存。

研发口径：

```text
final leaderboard Redis miss 后查 MySQL 并写缓存。
```

---

### 7.5 后台重算或数据修复后，最终榜缓存如何处理？

结论：需要显式删除对应活动的最终榜缓存。

说明：
最终榜默认不变，一旦人为修复或重算，就必须清理旧缓存，避免继续展示旧结果。

研发口径：

```text
重算 / 修复最终榜后，删除 final leaderboard cache。
```

---

### 7.6 Redis 删除或 version 递增失败怎么办？

结论：不回滚 MySQL，记录日志并依赖 TTL 或后续重试修复。

说明：
MySQL 已经保存事实数据，Redis 失败只影响缓存新鲜度，不应破坏主流程。

研发口径：

```text
Redis 失败不影响 MySQL 事务结果。
```

---

### 7.7 Redis 结算锁 token 是什么，为什么释放锁要校验 token？

结论：token 是锁持有者标识，释放锁时校验 token 是为了避免旧任务误删新任务持有的锁。

说明：
如果旧任务执行时间过长，锁过期后新任务获得锁，旧任务结束时直接 `DEL` 会删掉新任务的锁。校验 token 后再删除，可以避免误删。

研发口径：

```text
lock value = token。
释放锁时使用 Lua 脚本原子判断 token 后删除。
```

---

### 7.8 Redis 锁已经有了，为什么 MySQL 还要兜底？

结论：Redis 锁只能减少并发结算，不能保证最终正确性。

说明：
Redis 锁可能过期、释放失败、服务重启、任务执行超时。最终能判断活动是否已结算、最终榜是否已冻结的，必须是 MySQL 状态和事务。

研发口径：

```text
Redis 锁负责减少重复执行。
MySQL select for update + activity.status = finalized 负责最终兜底。
```

---

## 8. 活动截止与最终结算

### 8.1 为什么以服务端提交计分事务时间判断是否截止？

结论：因为客户端时间不可信，服务端事务时间是统一口径。

说明：
客户端时间可能不准或被篡改。使用服务端事务时间可以保证所有用户按同一标准判断是否截止。

研发口径：

```text
提交计分时，服务端判断 now < activity.end_time 才允许计分。
```

---

### 8.2 截止前进入手牌，截止后提交，为什么不计分？

结论：严格截止口径下，提交时间超过截止时间就不计分。

说明：
如果允许截止后提交，会引入宽限期、已开始手牌判定、最终结算延迟等新规则。7.15 第一版先保持简单清晰。

研发口径：

```text
进入时间不作为计分依据。
提交计分事务时间才是截止判断依据。
```

---

### 8.3 为什么 activity.status = finalized 要最后更新？

结论：因为 `finalized` 是最终榜已生成的信号，必须等榜单数据完整后再更新。

说明：
如果提前把 activity 更新为 finalized，但 `rank_no`、`is_final`、`finalized_at` 还没写完，前端可能认为最终榜已完成，实际榜单却不完整。

研发口径：

```text
先写 rank_no / is_final / finalized_at，再更新 activity.status。
```

---

### 8.4 Worker 重复执行最终结算，怎么处理？

结论：Redis 锁减少重复执行，MySQL 状态最终兜底。

说明：
多个 Worker 可能同时扫描到同一个活动。先用 Redis lock 降低并发，再在 MySQL 事务中锁定 activity 并判断是否 finalized。

研发口径：

```text
获取 Redis 锁 -> select activity for update -> 判断 finalized -> 执行结算。
```

---

### 8.5 活动状态枚举是否需要 ended？

结论：不一定需要，取决于是否要显式表达“已截止但未结算”。

说明：
如果不设计 `ended`，可以用 `end_time <= now AND status = published` 表示可结算活动。如果设计 `ended`，可以让状态更清晰，但会多一个状态流转。

研发口径：

```text
是否增加 ended 需要评审确认。
Worker 扫描条件以最终状态枚举为准。
```

---

### 8.6 Worker 扫描活动时，status 条件应该怎么写？

结论：应扫描“可结算状态”，不要用 `status != finalized` 这种宽泛条件。

说明：
`status != finalized` 可能把 draft、disabled、cancelled 等不应结算的活动扫进去，风险较大。

研发口径：

```sql
WHERE end_time <= NOW()
  AND status IN (<可结算状态>)
```

---

### 8.7 为什么用户访问时懒触发结算不适合作为主方案？

结论：因为如果没有用户访问，活动就不会及时结算。

说明：
最终榜结算是活动生命周期的一部分，不应依赖用户是否访问页面。懒触发可以作为补充，但不适合作为主流程。

研发口径：

```text
Worker 定时扫描为主。
用户访问懒触发最多作为补偿。
```

---

### 8.8 结算阶段为什么还要兜底补齐 leaderboard_result？

结论：用于处理极端异常数据，不是主流程。

说明：
正常情况下，用户完成第 N 手时已经同事务写入 `leaderboard_result`。结算阶段补齐只是为了处理历史数据或极端异常数据。

研发口径：

```text
最后一手入榜是主流程。
Worker 补齐只是异常补偿。
```

---

### 8.9 为什么结算任务要先把未完成用户更新为 expired？

结论：为了明确截止后未完成用户不能再继续活动。

说明：
这样用户状态和榜单边界清晰，最终榜只处理 completed 用户。

研发口径：

```text
截止后未完成 entry 更新为 expired，不进榜。
```

---

## 9. 异常场景与不做范围

### 9.1 最后一手响应丢失，为什么 resume 能恢复到排行榜？

结论：因为服务端事务已提交，MySQL 中已有 completed entry 和 leaderboard_result。

说明：
前端是否收到响应不影响服务端事实数据。重新进入后，resume 以 MySQL 状态为准，能判断用户已经完赛并返回榜单入口。

研发口径：

```text
resume 根据 MySQL 判断完赛状态，不依赖客户端响应。
```

---

### 9.2 最后一手事务失败时，为什么要整体回滚？

结论：避免留下半完成数据。

说明：
如果只写入 `hand_score`，但 `entry` 没完成或 `leaderboard_result` 没写入，会导致后续恢复、排行榜、最终结算口径不一致。

研发口径：

```text
最后一手计分、entry completed、leaderboard_result 同事务成功或同事务失败。
```

---

### 9.3 Redis 缓存失效失败为什么不回滚 MySQL？

结论：Redis 不是事实源，不能因为缓存失败回滚比赛结果。

说明：
MySQL 成功就代表业务事实成功。Redis 失败最多造成短时间看到旧缓存，不能反过来影响比赛结果。

研发口径：

```text
MySQL 成功即事实成功。
Redis 失败记录日志，依赖 TTL 或重试修复。
```

---

### 9.4 为什么不做截止宽限期？

结论：宽限期会引入复杂时间口径，影响结算边界。

说明：
一旦允许截止后提交，就要定义宽限时间、已开始手牌判断、结算延迟、最终榜冻结时间等新规则。7.15 第一版先采用严格截止。

研发口径：

```text
7.15 严格按服务端提交计分事务时间判断。
```

---

### 9.5 为什么不做排名快照表和历史当前榜快照？

结论：当前榜动态查询，最终榜冻结即可满足 7.15。

说明：
排名快照主要用于历史复盘和运营分析，当前没有明确需求。为了降低复杂度，7.15 不做快照表。

研发口径：

```text
需要历史排名变化时，再新增快照表。
```

---

### 9.6 为什么 7.15 不做复杂异步消息队列？

结论：当前核心流程用 MySQL 事务更直接、更容易保证一致性。

说明：
如果 submit hand 后异步入榜，会引入最终一致性、补偿、重试、消息丢失等复杂问题。7.15 第一版优先保证主流程同步闭环。

研发口径：

```text
submit hand 主流程同步完成计分和入榜。
Worker 只负责最终结算。
```

---

### 9.7 data_inconsistent 是否需要后台告警？

结论：建议需要。

说明：
`data_inconsistent` 表示核心状态不一致，可能影响用户进度或排行榜结果，应记录日志并告警，方便及时修复。

研发口径：

```text
data_inconsistent 记录错误日志，并建议接入告警。
```

---

## 10. 待确认项

### 10.1 是否允许部署独立 Worker？

当前建议：建议允许。
影响：影响最终结算触发方式。

---

### 10.2 活动状态枚举是否包含 ended？

当前建议：需要确认。
影响：影响 Worker 扫描条件。

---

### 10.3 当前榜是否限制最大翻页深度？

当前建议：建议限制 `pageSize`，最大页码待确认。
影响：影响深分页性能。

---

### 10.4 我的当前排名是否必须展示？

当前建议：默认不自动计算，按需计算。
影响：影响当前榜接口性能。

---

### 10.5 后台手动触发结算是否必须做？

当前建议：建议作为补偿入口。
影响：影响测试、运维和异常修复。

---

### 10.6 同分排序是否只按完成时间？

当前建议：当前按完成时间。
影响：影响最终排名稳定性。

---

### 10.7 total_score 是否使用平均分？

当前建议：默认 `AVG(score)`。
影响：影响榜单排序和测试用例。

---

### 10.8 活动截止后是否允许查看已完成手牌结果？

当前建议：建议允许查看，不允许继续提交。
影响：影响前端展示。

---

### 10.9 排行榜页是否区分当前榜和最终榜文案？

当前建议：建议区分。
影响：避免用户误解排名是否已冻结。

---

### 10.10 data_inconsistent 是否需要后台告警？

当前建议：建议需要。
影响：影响异常数据发现和修复。

---

## 11. 核心口径总结

7.15 的补充说明可以归纳为 10 个核心口径：

```text
1. MySQL 是唯一事实源。
2. Redis 只做缓存和结算锁。
3. 用户参赛后生成固定 N 手牌。
4. submit hand 不是用户提交分数，而是触发服务端计分。
5. 同一 user_hand 只能计分一次。
6. 最后一手计分、entry 完成、leaderboard_result 写入必须同事务。
7. 当前榜动态查询，不落库 rank_no。
8. 最终榜截止后冻结 rank_no。
9. 断线恢复只恢复活动进度，不恢复单手中途状态。
10. 7.15 不做复杂回放、快照、宽限期、Sorted Set 主榜和复杂异步链路。
```

这版补充说明的定位是评审答疑手册。
主设计文档说明“怎么做”，本文档说明“为什么这么做”和“边界怎么解释”。
