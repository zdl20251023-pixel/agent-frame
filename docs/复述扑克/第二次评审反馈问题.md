评审同步事项核心确认
题目表，分数表合成一张表
用户start_hand内部实现需要注意减少查表次数
用户start_hand直接走创建牌桌流程，不能再返回drill_info让前端再触发创建牌桌流程
排行榜不用数据库表，改成全用redis实现
woker结算下一期考虑
submit_hand流程取消，改成直接game牌局结束直接更新数据库流程
获取排行榜和当前排名需要考虑是一个接口，还是2个接口？
复式每局牌需要在一个table上面玩，共用同一个tableId
前端admin移植复盘流程，需要写转原始数据到drill玩法需要的数据的转换接口
是否有理解不对和缺少的   
算法细节等具体实现的时候考虑


后台表变成只留下2个表
duplicate_match_user_hand 和 duplicate_match_hand_score 合并成一个表，找个合适的名字
排行榜不用数据库表，改成全用redis实现，因此duplicate_match_leaderboard_result表设计删除
排行榜目前只做总bb输赢的榜单。 不做打分的排行榜，因此
