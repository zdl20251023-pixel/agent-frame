# PRD 可评估最低标准模板

## 1. 需求目标

请用简短说明讲清楚这个需求为谁解决什么问题，以及完成后希望达到什么结果。

- 目标用户 / 使用对象：

- 当前问题：

- 目标结果：

- 本期不包含：

---

## 2. 主流程

按照范围从大到小说明流程：先写整体流程，再写各功能模块流程，最后标出会影响研发实现的关键节点。

- 整体流程：

- 模块流程：
  - 模块 1：
  - 模块 2：
  - 模块 3：

- 关键节点：

---

## 3. 涉及范围

确认本次需求会影响哪些范围；涉及的部分只写一句改动说明，不展开技术方案。

主要影响对象：

填写：

| 范围 | 是否涉及 | 改动类型 | 简单说明 |
|---|---|---|---|
| 用户端 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |
| 管理后台 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |
| 配置项 / 配置表 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |
| 服务端逻辑 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |
| 其他系统 / 第三方 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |
| 已有数据 / 历史流程 | 是 / 否 | 新增 / 修改 / 复用 / 待确认 / 不涉及 |  |

改动类型说明：

- 新增：原来没有，需要新做；
- 修改：原来已有，这次要调整；
- 复用：沿用已有能力，只是接入或使用；
- 待确认：不确定是否影响，需要研发评估；
- 不涉及：本次不影响这个范围。

---

## 4. 关键规则

请按“业务范围 → 功能模块 → 具体对象 → 主规则 → 子规则 → 特殊情况 → 处理结果”的顺序说明，避免规则散着写。

### 规则：

如有多条规则，可复制本结构。

- 规则对象：
  - 业务范围：
  - 功能模块：
  - 具体对象：

- 主规则：

- 子规则：
  - 
  - 
  - 

- 特殊情况：

- 处理结果：

---

## 5. 验收标准

请说明本次需求需要验收哪些范围、哪些场景算通过，以及哪些情况不能算通过。

### 5.1 验收范围

填写本次需要验收的主要部分；不涉及的范围写“不涉及”，不确定的范围写“待确认”，避免研发误判为遗漏。

- 用户端：
- 管理后台：
- 配置项 / 配置表：
- 服务端逻辑：
- 数据 / 状态：
- 其他系统 / 第三方：

### 5.2 验收场景

按具体场景说明“什么操作 / 条件下，应该出现什么结果”。

| 场景 | 操作 / 条件 | 预期结果 |
|---|---|---|
| 场景 1 |  |  |
| 场景 2 |  |  |
| 场景 3 |  |  |

### 5.3 不通过情况

说明哪些情况出现时，不能算验收通过。

- 
- 
- 

---

## 6. 待确认点

请列出当前还不明确、需要后续确认的问题；每个问题说明影响范围、负责人和确认节点，避免研发靠猜。

| 待确认问题 | 影响范围 | 当前建议 / 默认处理 | 负责人 | 确认节点 |
|---|---|---|---|---|
| 问题 1 |  |  |  | 开发前 / 开发中 / 联调前 / 验收前 |
| 问题 2 |  |  |  | 开发前 / 开发中 / 联调前 / 验收前 |
| 问题 3 |  |  |  | 开发前 / 开发中 / 联调前 / 验收前 |

确认节点可选：开发前 / 开发中 / 联调前 / 验收前。

---

## 7. 异常处理

请说明已知的主要异常；不要求一次性覆盖所有异常，开发过程中发现未覆盖异常时，由研发记录并和产品确认处理效果。

### 7.1 已知异常

按异常发生的位置填写，说明异常情况、处理方式和用户 / 系统表现。

| 异常发生位置 | 异常情况 | 处理方式 | 用户 / 系统表现 |
|---|---|---|---|
| 位置 1 |  |  |  |
| 位置 2 |  |  |  |
| 位置 3 |  |  |  |

### 7.2 未覆盖异常处理

开发过程中发现未覆盖异常时，先记录异常场景，再和产品确认最终处理效果。

| 未覆盖异常 | 发现阶段 | 影响范围 | 确认人 | 最终处理方式 |
|---|---|---|---|---|
| 异常 1 | 开发中 / 联调中 / 验收中 |  |  |  |
| 异常 2 | 开发中 / 联调中 / 验收中 |  |  |  |
